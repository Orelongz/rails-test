<% if is_admin? %>
  <%= link_to 'Log Out', logout_path %>
<% else %>
  <p>
    Are you an admin? &nbsp; <%= link_to 'Log In', login_path %>
  </p>
<% end %>

<% if alert %>
  <p id="error_explanation"><%= alert %></p>
<% end %>

<p id="notice"><%= notice %></p>
